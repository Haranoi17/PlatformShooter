#Exiting when script is run directly
if __name__ == "__main__":
    print("This file is not ment to be run directly (exiting)")
    exit()

#Printing some debug message when importing package
print("Debug: Game library imported properly")

import pygame